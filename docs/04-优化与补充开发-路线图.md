# LumoRPA 优化与补充开发路线图

> 生成于 2026-05-29。基于对 8 个 crate + Tauri 桌面端(~21.6k 行)的全量子系统审计 + 关键结论的人工 grep 复核。
> 本文既是分析报告,也是执行清单。状态图例:`[ ]` 待办 · `[~]` 进行中 · `[x]` 已完成。

## 总体判断

架构扎实的 M1/M2 阶段本地优先 RPA 运行时。错误模型统一、能力沙箱/网络开关基本健全、录制器与"影刀式编辑器"均已真正实现(README 的 placeholder 标签过时)、actions 多达 91 个(文档承诺 50)。

**但**:最新 P0 提交的招牌功能(AI 痕迹闭环 + 多模态抽取)因"最后一公里"未接线而**实际不运行**,且存在数个真实安全边界漏洞。

---

## 🔴 P0 — 必须先修(正确性 / 安全 / 招牌功能失效)

- [x] **P0-1 AI 钩子全链路未接线**(已人工核实)✅ 已修复
  - 新增 `lumo_ai::build_hook_provider` 工厂(3 单测);在全部 6 个 CLI runner(run/serve×3/hotkey/mcp/skills)+ 桌面 `execute_flow` 接 `attach_ai_hooks`;新增 lumo-core e2e 回归测试(有 provider 则 heal 触发、无 provider 则失败为终态)。
  - `with_ai_provider()`(`crates/lumo-core/src/vm.rs:82`)**零调用者**;`AiHooks`(`crates/lumo-ai/src/helpers.rs:336`)从未构造;9 处生产 `FlowVm::new` 都没链 `.with_ai_provider(...)`。
  - `effective_ai_mode`(`vm.rs:1043`)在 `ctx.ai_provider().is_none()` 时直接返回 Off ⇒ heal_selector / extract_visual / decide / vision_locate 全部短路,一次都不触发。
  - **修复**:在每个 runner 的 `FlowVm::new` 站点按 `flow.metadata.ai.enabled` 链 `.with_ai_provider(Arc::new(AiHooks::new(router, budget)))`,加端到端集成测试。
- [x] **P0-2 能力沙箱路径不规范化(`..`/软链接逃逸)**(已人工核实)✅ 已修复
  - `normalize_path` 增加 `lexical_clean`(折叠 `.`/`..`)在前缀匹配前执行;故意不 canonicalize 软链接以避免 macOS `/tmp`→`/private/tmp` 误判。3 单测覆盖逃逸拒绝/内部点段放行。
- [x] **P0-3 Tauri 文件 IPC 无沙箱 + Webview 无 CSP** ✅ 已修复
  - 新增纯函数 `resolve_within`(读限制到 LUMO_HOME + 只读 examples)与 `resolve_write_within`(写限制到 LUMO_HOME,父目录规范化校验),应用到 `read_flow_source`/`save_flow_source`/`get_flow_capabilities`/`duplicate_flow`/`inspect_flow` 全部取路径命令(`delete_flow` 原已守卫)。`tauri.conf.json` 设严格 CSP:`script-src 'self'`(封堵 62 处 innerHTML 的 XSS 提权)、`style-src 'unsafe-inline'`(兼容 60 处内联样式)、`img/frame-src data:`(产物预览)、Tauri v2 `ipc:` 源。5 单测覆盖越界/`..`/不存在路径。
- [x] **P0-4 `control.parallel` 并发分支共享可变状态** ✅ 已修复
  - `StepCtx` 拆出共享 `seq: Arc<AtomicI64>`(保持 `step_runs` 主键唯一),新增 `fork()`(深拷贝 vars/bindings/steps 隔离)+ `merge_branch()`(回并 outputs/vars/stats);`run_parallel` 改用 fork+merge。新增确定性回归测试(分支只见自己的 item)。
- [x] **P0-5 `skill.invoke` 越权 + 无递归限制** ✅ 已修复
  - 新增 `lumo_core::clamp_capabilities`(子 flow 能力裁剪到调用者子集,2 单测);`FlowVm` 增加 `with_skill_depth`/`with_capability_override`;`StepCtx` 携带 `skill_depth`;`skill.invoke` 检查 `MAX_SKILL_DEPTH=8` 并以裁剪后的能力运行子 flow。

## 🟠 P1 — 健壮性 / 可用性 / 工程化

- [x] **P1-1 VM 无取消/超时** ✅ 已修复
  - 新增 `lumo_core::CancelToken`(基于 `watch`,`cancel()`/`cancelled()`/`is_cancelled()`);`FlowVm` 增 `with_cancel`/`with_step_timeout`;`StepCtx` 携带 cancel/timeout 并在 `fork` 中传递。`execute_step` 每步前检查取消、用 `select!`(biased) 在执行中并发监听取消/`tokio::time::sleep` 超时;取消→`ExecError::Cancelled`、超时→`ExecError::Timeout`。run 结束按结果落 `cancelled`/`failed`/`ok` 状态。`lumo run` 接 Ctrl-C→取消(退出码 130)。4 单测覆盖运行中取消/运行前取消/超时/无限制正常。
- [x] **P1-2 浏览器进程失败时不回收**(`browser.rs:157`)✅ 已修复
  - 新增 `lumo_core::RunTeardown` 钩子 + `ActionRegistry::register_teardown`;`FlowVm::run` 在流程结束(成功/失败均执行)按 `run_id` 调用全部钩子。`browser.rs` 注册 `BrowserTeardown`,`Session` 改 `tokio::sync::Mutex<Browser>` + 可中止的 handler 任务,`teardown_session` 做 `close().await`→(失败则 `kill()`)→`wait()` 回收子进程。新增 lumo-core 端到端测试(成功/失败两路均触发 teardown)+ lumo-actions 真 Chrome 集成测试(`#[ignore]`,本地已验证回收)。
- [x] **P1-3 加密 vault 仅有表无实现** ✅ 已修复
  - 新增 `lumo-storage::vault` 模块:`VaultIdentity`(age X25519 身份,generate/load/save-0600/recipient)+ `encrypt`/`decrypt`(age 一次性 API)+ `Vault<'a>` 门面(put/get/delete,JSON 对象整体加密)+ `list`(仅元数据,永不解密)+ `get_field`(运行时单字段解密)。`Repo` 加 `vault_put/get/list/delete` 裸行方法(`vault_items` 表已在 baseline DDL)。
  - `lumo-core`:`StepCtx`/`FlowVm` 经 `with_vault` 携带不透明 `Arc<VaultIdentity>`(core 不直依赖 age);`${{ vault.* }}` 解析重构为 `VaultResolver`——**env 优先**(`LUMO_VAULT_*`,向后兼容/CI 覆盖)、**加密 store 回退**(repo+identity 均在时调 `get_field`)、都无则报错;身份缺失则优雅降级为纯 env。模板层 `preprocess_vault` 用 `{% raw %}` 将 `{{ vault.* }}` 真正字面量化(秘密绝不进入实时模板引擎/快照/日志)。
  - CLI 新增 `lumo vault`(init/add/get/list/rm/path):密文仅经隐藏 prompt(rpassword)或 `--stdin`,**绝不**上 argv;`get` 默认掩码、`--reveal` 出明文;`list` 永不出明文。run/serve/hotkey/mcp 运行器从 `$LUMO_HOME/age-identity.txt`(可经 `LUMO_VAULT_IDENTITY` 覆盖)加载身份注入 VM;子流程运行器(skills)保持纯 env。
  - 依赖:`age = "0.11"`(纯 Rust,无 C,利好交叉编译)+ `rpassword = "7"`。测试:lumo-storage 加解密/CRUD/门面往返 13 例 + lumo-core 解析优先级 6 例 + VM 端到端 + lumo-dsl 占位符边界 + lumo-cli 纯逻辑单测。
- [x] **P1-4 AI hook 调用不进成本账本** ✅ 已修复
  - 新增 `lumo_core::AiCallUsage` + `AiHookProvider::take_usage`(默认空实现 → 旧 impl/测试 stub 不破);`AiHooks` 内置 `Mutex<Vec<AiCallUsage>>` 累加器,5 个 helper(heal_selector/extract_visual/decide/vision_locate/diagnose)计时 + 算价后入账。成本表 `cost_micro` 抽到 `lumo-ai::cost`,与 `ai.chat` 动作共用(消除重复)。
  - VM 在 5 个钩子点 drain 用量写 `ai_calls`(run_id/step_id 取自 ctx),并把 tokens/latency/cost 折进 `_ai.usage` 痕迹;`vision_locate`(解析器无 ctx)由动作成功后兜底 drain。新增 lumo-core e2e(账本行 + `_ai.usage` + rollup)、lumo-ai wiremock(累加 + 算价)、`ai_usage_aggregate` 单测。
  - 注:`control.parallel` 并发分支共享累加器 → 运行级总量精确(不漏不重),但 step 归属可能串到兄弟分支(已在 `AiHooks::usage` 注明)。
- [x] **P1-5 多模态无图片大小限制 + provider 无 429 重试** ✅ 已修复
  - `helpers.rs` 加 `MAX_IMAGE_BYTES=4MiB` 守卫(`extract_visual`/`vision_locate` 编码前校验);vision 解析失败改 `tracing::warn`(含截断原文)再回退默认值;`provider.rs` 加 `send_with_retry`(429/5xx + 瞬时网络错误指数退避,最多 3 次,4xx/解码错误保持终态)。wiremock 测试 4 例(重试成功/401 不重试/超大图拒绝)。
- [x] **P1-6 `lumo run` 失败仍退出 0**(`crates/lumo-cli/src/cmd/run.rs`)✅ — 失败时 `process::exit(1)`。
- [x] **P1-7 存储无 schema 迁移框架 + 无 busy_timeout** ✅ 已修复
  - `repo.rs` 加 `LATEST_USER_VERSION` + `run_migrations`(按 `PRAGMA user_version` 有序应用,已现版本短路);`init_connection` 每连接显式设 `busy_timeout(5s)`/`foreign_keys=ON`/`journal_mode=WAL`/`synchronous=NORMAL`;旧 `step_runs` 重建为 v1、baseline DDL 为 v2(修正了 DDL-先于-重建的列缺失 bug)。新增 5 例迁移测试(全新库/幂等重开/旧库原地迁移/PRAGMA 生效)。
- [x] **P1-8 测试覆盖不足** ✅ 已补
  - `lumo-actions` 新增 15 个集成测试文件(共 85 例),全部经真实 `ActionRegistry` 跑动作(与 VM 同路径):纯逻辑族 string/math/list/json/hash+util/regex/data/date,以及 control(`if` 真值表/`fail` 用户消息/`set_var` 写 ctx/loop+block marker 空操作);带资源族经 tempdir + 能力沙箱覆盖 file(读写/追加/越界拒绝)、csv(解析/转写/文件往返)、db(sqlite exec→query 往返 + limit 截断)、excel(xlsx 写行→读行往返/追加);http 用 wiremock 起本地服务(状态/JSON/纯文本解析 + 无网授权拒绝)。共享 `tests/common/mod.rs` 暴露 `run/ok/run_with/ok_with/fs_caps`。browser/vision/selectors/mcp/recorder 依赖真实 Chrome/MCP server,归 e2e 不在此列。
  - CI 加 `coverage` job:`dtolnay/rust-toolchain` + `llvm-tools-preview` + `taiki-e/install-action` 装 `cargo-llvm-cov`,跑 `--workspace --exclude lumorpa-desktop --ignore-filename-regex '(browser|vision|selectors)\.rs$|lumo-recorder/src' --fail-under-lines 50 --summary-only`(排除 CI 无法覆盖的浏览器/Tauri 代码);`build-test` 的 lumo-actions 步骤由 `--lib` 改为跑全部集成测试。下限取保守初值按棘轮处理——首次 CI 打印真实 TOTAL 后只升不降。
- [x] **P1-9 CI 缺供应链/覆盖率/MSRV/信创验证** ✅ 已补
  - 供应链:新增根 `deny.toml` + CI `cargo-deny` job(`EmbarkStudios/cargo-deny-action@v2` 跑 `check all`)。许可证白名单由 `cargo deny list` 实跑全树(all-features)推导——纯许可 + 文件级弱 copyleft(MPL-2.0)+ webpki-roots 的 CDLA-Permissive-2.0;GPL/AGPL 强 copyleft 缺席,靠"未列即禁"兜底。`yanked = deny`;`unmaintained = "workspace"`(只盯直接依赖的失维,深层传递的失维如 proc-macro-error 不红 CI,但安全公告始终阻断)。licenses/bans/sources 本地实跑通过。
  - 覆盖率:见 P1-8(`coverage` job,`cargo-llvm-cov --fail-under-lines`)。
  - MSRV:新增 `msrv` job,`dtolnay/rust-toolchain@1.83.0` 跑 `cargo check --workspace --exclude lumorpa-desktop`(发行库/二进制按声明的 1.83 编译;dev-only 测试依赖不在 MSRV 契约内,故不带 `--all-targets`)。
  - 信创/交叉:新增 `cross-check` 矩阵 job——aarch64-unknown-linux-gnu 为硬门禁(apt 装 `gcc-aarch64-linux-gnu`,经 `CC_<triple>` 接入交叉 C 编译器,因 rusqlite bundled SQLite + ring 需为目标编译 C);loongarch64-unknown-linux-gnu 走 `continue-on-error` 尽力而为(交叉 C 工具链与 ring/libsqlite3-sys 的该目标支持在 runner 上尚不稳定,红了仅作信息)。`build-test` 全工作区构建本就覆盖 desktop,交叉/MSRV job 显式 `--exclude lumorpa-desktop`(Tauri 系统库与可移植性无关)。
- [x] **P1-10 解析器不拒未知字段 + 模板缺失变量静默变空** ✅ 已修复
  - lumo-dsl 11 个 spec/parser 结构(Flow/Metadata/FlowSpec/IoDecl/Capabilities/Trigger/Step/RetrySpec/StepAi/FlowAi/AiBudget)加 `deny_unknown_fields`(无 flatten 冲突,11 个示例全部仍可解析);模板 `build_env` 设 `UndefinedBehavior::SemiStrict`(`{{ missing }}` 报错而非静默空,`is defined` 守卫仍可用)。新增 10 单测(全工作区测试通过)。
- [x] **P1-11 桌面端锁中毒崩溃**(`lib.rs` recorder 锁)✅ — 改 `lock().unwrap_or_else(|e| e.into_inner())`(3 处)。

## 🧩 补充开发(功能缺口)

**Actions**
- [x] **F-1 桌面自动化（坐标 + 图像驱动）** `desktop.*`（L）✅ 已完成（MVP；原生截图/控件树延后）
  - 用户定调「坐标 + 图像」而非可访问性树。5 个动作经 `rdev` 模拟 OS 输入，均过**新增 `desktop`
    能力闸门**（粗粒度类别 `mouse`/`keyboard`/`*`，`ensure_desktop` + `CapKind::Desktop`，child
    上下文按 `matches_any` clamp）：`desktop.move`（指针绝对坐标）、`desktop.click`（left/right/
    middle，可双击，可先移动）、`desktop.scroll`（滚轮 dx/dy）、`desktop.key`（组合键解析
    `ctrl+c`/`cmd+shift+t`/`enter`…）、`desktop.type`（**剪贴板粘贴**：arboard 写入 + 模拟
    Cmd/Ctrl+V，对中文/Unicode 安全 —— rdev 逐键产不出中文）。schemars 派生（5 个 `*In` +
    `deny_unknown_fields`，`ClickButton` 派生枚举内联）入 `schema_derive` 门禁（feature 开启时纳入）。
  - **信创**：桌面输入无纯 Rust 实现（必走 macOS CGEvent / Linux XTest / Windows SendInput 的 C
    API），故整族置于**默认关闭**的 `desktop` feature（`rdev` optional），信创核心 / headless
    aarch64 交叉编译不受影响，桌面端 `--features desktop` 开启；与 CLI 既有 `hotkey-rdev` 同策。
  - **图像驱动点击**由 `image.locate → desktop.click` 组合达成（locate 回传 center_x/center_y 直接
    喂 click 的 x/y），见 `examples/desktop-click-image.lumoflow.yaml`。
  - `tests/desktop.rs`：能力闸门 + 类别粒度 + 入参/组合键解析校验在 CI 跑（均在闸门/解析阶段返回，
    不触 rdev、不动鼠标）；真实触发标 `#[ignore]` e2e（需显示器 + macOS 辅助功能授权）。
  - **延后**：原生整屏截图（xcap 需 LLVM/pipewire/libxcb 破坏信创）→ haystack 暂由 browser.screenshot/
    外部提供；`desktop.click_image` 合一动作（待原生截图）；窗口/控件树枚举（可访问性树，用户已选不走）。
- [x] **F-2 图像/模板匹配**(L)✅ 已完成(OCR 文本识别已实现)
  - `image.locate`(在大图/截图中用归一化互相关 NCC 定位模板小图，返回左上角 + 中心坐标 + 分数 + `found`，中心坐标可直接喂 F-1 坐标点击）+ `image.compare`（两张**同尺寸**图整体相似度，1≈一致）。纯 Rust `image` 0.25 + `imageproc` 0.25（均 MIT，无 C 依赖，信创/aarch64 交叉友好；image features 收窄 png/jpeg/bmp，png 覆盖 `browser.screenshot` 产物）。均 LOCAL 不触网，先过 `fs.read` 能力闸门再读盘，解码 + 匹配挪 `spawn_blocking` 不阻塞执行器。schemars 派生（`LocateIn`/`CompareIn` + `deny_unknown_fields`）入 `schema_derive` 门禁。`examples/image-locate.lumoflow.yaml` + `tests/image_match.rs`（6 例：合成确定性噪声图裁剪模板验证坐标精确召回 + 尺寸不匹配/超尺寸/未授权路径/未知字段拒绝）。
  - **OCR 文本识别 ✅ 已实现**（审计 2026-06-05 复核）：`image.ocr` 双路径 —— 云端走 AI provider 的 vision 模型（router `ocr_model`/`vision_model`），本地走 ModelScope/Python 预设（`LUMO_OCR_PYTHON`，默认 `python3`，如 `modelscope/ZhipuAI/GLM-OCR`、`modelscope/PaddlePaddle/PaddleOCR-VL-1.6`），桌面 Models 页经 `download_ocr_model` 命令拉取预设（`crates/lumo-actions/src/image_match.rs:32/283`、`ocr_models.rs`、`apps/desktop/src-tauri/src/lib.rs:1305-1322`）。**仍延后的只有纯 Rust 准确中文 OCR 引擎**：当前无此引擎（ocrs/RTen 仅拉丁文；准确中文需 MNN/C++ 或 tesseract 的 C 依赖，违反信创/交叉），故本地推理依赖外部 Python 运行时。待上游成熟再补纯 Rust 引擎。
- [x] **F-3 email(SMTP/IMAP)**(M)✅ 已完成
  - `email.send`(SMTP,lettre + rustls/ring 隐式 TLS)+ `email.fetch`(IMAP,async-imap + tokio-rustls/ring,取最近 N 封头);均 schemars 派生(`SendIn`/`FetchIn` + `deny_unknown_fields`)入 `schema_derive` 门禁。secrets 经 vault/env 预解析为明文入参,network 门禁(附件另过 fs-read)。依赖锁 lettre `=0.11.19`(0.11.22 起 MSRV 1.85 > 本仓 1.83)、全程 rustls 避 openssl C 依赖(信创/交叉)。`examples/email-send.lumoflow.yaml` + `tests/email.rs`(7 例)。
- [x] **F-4 PDF 读写/抽取**(M)✅ 已完成
  - `pdf.extract_text` / `pdf.info`(页数 + 廉价元数据)/ `pdf.write`(文本渲染存盘),纯 Rust lopdf `0.36`(MIT,无 C 依赖;0.40 需 rustc 1.85)。fs-read/fs-write 门禁。schemars 派生(`ExtractTextIn`/`InfoIn`/`WriteIn`)入门禁。`examples/pdf-extract.lumoflow.yaml` + `tests/pdf.rs`(8 例)。
- [x] **F-5 剪贴板 clipboard.get/set**(S)
- [x] **F-6 FTP/SFTP/对象存储**(M)✅ 已完成(SFTP 延后)
  - `ftp.upload`/`ftp.download`(suppaftp,tokio + tokio-rustls-ring 纯 Rust)+ `s3.put`/`s3.get`(S3 兼容 MinIO/OSS,自签 AWS SigV4 复用本 crate 的 reqwest/hmac/sha2,见 `transfer/sigv4.rs`——刻意不引 rust-s3/aws SDK 以避 aws-lc-sys C 依赖破坏 aarch64 交叉)。schemars 派生(4 个 `*In` 补齐 `deny_unknown_fields`)入门禁,fs + network 门禁。`examples/s3-download.lumoflow.yaml` + `tests/transfer.rs`(12 例)。SFTP 延后(suppaftp 高层 API 暂缺,见模块头)。
- [x] **F-7 ZIP/归档**(S)
- [x] **F-8 通知(钉钉/飞书/企微/webhook)**(S~M)
- [x] **F-9 显式 `browser.wait`(visible/clickable/text)**(S)
- [x] **F-10 浏览器动作补全**(cookie/iframe/上传/JS eval/截图存盘/滚动/悬停/下拉/标签页)(M)
  - ✅ 已补 7 个 action(均 schemars 派生 + 入 `schema_derive` 门禁):`browser.eval`(JS→JSON)、`browser.screenshot`(整页 PNG 存盘,fs-write 能力在取 session 前前置 gate,失败快且可 CI 测)、`browser.scroll`(元素 `scroll_into_view` / 窗口 top·bottom·delta)、`browser.hover`(复用 click 的解析器 + 视觉兜底)、`browser.select`(`<select>` 按 value/label/index,经 `data-lumo-resolved` 标记驱动 + 派发 input/change)、`browser.cookies`(读,可按名过滤)、`browser.set_cookie`(写,url/domain 缺省回退当前页 URL)。新增 `tests/browser_actions.rs`(能力/入参校验在 CI 跑,行为路径 `#[ignore]` e2e)。
  - ✅ part 2(标签页 + 上传 + iframe-eval,均 schemars 派生 + 入 `schema_derive` 门禁):`browser.tabs`(列出全部 tab 的 target_id/url/title/active)、`browser.tab`(`op: activate|close`,按 `target_id` 或 `url_includes` 寻址 —— chromiumoxide 内部 `pages()` 遍历 `HashMap` 顺序不稳,故**不按 index**;`browser.open` 本就「开新 tab + 切过去」,session 的 `page` 指针即活动 tab,`activate` 仅重指)、`browser.upload`(裸 CDP `DOM.setFileInputFiles`,从 `resolve_element` 的 `Element.backend_node_id` 取节点,各路径在取 session 前过 fs-read 门禁、失败快可 CI 测)。iframe 采「eval 进帧」:`browser.eval`/`browser.extract` 加可选 `frame:{url_includes|name}`,经 `frame_execution_context` + `Runtime.evaluate(context_id)` 在该帧执行上下文跑 JS(JS 抛错显式报出而非静默 null)。**已知限制**:DOM 选择器策略(click/type)入帧 chromiumoxide 0.7 高层 API 不支持,需走 `frame:` 的 JS 路径(留待 L 类「完整帧切换」)。新增 `examples/browser-tabs.lumoflow.yaml`;`browser_actions.rs` +9 CI 校验、+3 `#[ignore]` e2e sketch。整 workspace 459 测试绿、clippy `-D warnings` 干净。
- [x] **F-11 http.download/上传 + 响应大小上限**(S)
- [x] **F-12 数据表/列表谓词 filter/group-by/join**(M)

**VM / DSL**
- [x] **F-13 持久化断点续跑**(读回 `step_runs`/`input_hash`,跳过已完成步)(L)
- [x] **F-14 真正的布尔表达式求值**(`when`/`control.if`)(M)
- [x] **F-15 子流程 `flow.call`**(M)
- [x] **F-16 `retry.on` 错误类型匹配 + VM 强制 AI budget**(S)
- [x] **F-17 step `with:` 对 action JSON Schema 校验**(M)

**前端 / 桌面**
- [x] **F-18 Magic Prompt 面板**（M）✅ 已完成（后端共享 core + UI；运行时需 `tauri dev` 验证）
  - NL→Flow 生成核心从 CLI 提取到 `lumo_ai::copilot`（`generate_flow` + extract/validate/retry，
    9 个单测随迁；lumo-ai 新增 `lumo-dsl` 直接依赖，沿 lumo-ai→lumo-core→lumo-dsl 无环）：`lumo
    copilot`（CLI）与桌面面板共享同一逻辑。桌面新增 `generate_flow` Tauri 命令（载 providers→
    AiRouter，活动 provider 默认模型或 `<provider>/<model>` override，校验空 prompt / 无 provider /
    LLM 网络开关）。前端 `js/magic-prompt.js` 抽屉（✨ 按钮，复用 ai-drawer 样式，守 P0-3 CSP
    `script-src 'self'`）：输入描述 → 生成 → 另存为新流程并在编辑器打开。
  - 后端 `cargo test -p lumo-ai` 绿、`lumo-cli` / `lumorpa-desktop` 编译绿；前端 ESLint 干净。
    **运行时 UI 冒烟需 `tauri dev`（前端无自动化测试，见 F-21）。**
- [x] **F-19 变量 Watch 面板**（M）✅ 已完成（后端持久化 + UI；运行时需 `tauri dev` 验证）
  - 后端：每个 step 持久化 `vars`（`set_var`）环境快照。`step_runs` 新增 `vars_json TEXT`
    列（schema baseline DDL + 受保护的 v2→v3 迁移：先 `PRAGMA table_info` 查列再 `ALTER`，
    所以新库走 baseline、旧 v2 库才补列，二者皆幂等；`LATEST_USER_VERSION` 2→3，
    `migrations.rs` 的硬编码版本常量同步更新）。VM `persist_step`（已持 `ctx: &StepCtx`）
    直接写 `ctx.vars_snapshot()`，无需改 `StepPersist` 及其调用点。`StepRunRow.vars_json`
    经 `insert_step`/`list_steps` 往返；`storage_smoke` 新增 `step_vars_json_round_trips`
    单测（含 close/reopen 跑通 v2→v3 迁移）。
  - 桌面：`StepRunDto` 新增 `vars_json`（serde camelCase → 前端 `varsJson`），`step_dto`
    映射；`run_flow`/`run_step`/`show_run` 三路径均经 `list_steps→step_dto` 回读，故实时运行
    与历史回放都带 `varsJson`。
  - 前端：`js/runs.js` 的 X-07 时光机逐步检视器（`showStepDetail`）新增 `renderVarsWatch`，
    擦洗时间轴即可看变量随 step 演进（标量内联、对象/数组 pretty-JSON，旧运行 `varsJson`
    为 `null` 时不渲染、面板保持原样；全程 `html()` 转义守 P0-3 CSP）。
  - 后端 `cargo test -p lumo-storage`（migrations 5 + storage_smoke 4 含新测 + vault 13）、
    `lumo-core` 全绿、`lumorpa-desktop` `cargo check` 绿；前端 `eslint .` 干净。
    **运行时 UI 冒烟需 `tauri dev`（前端无自动化测试，见 F-21）。已知范围：变量 Watch 接在
    时光机检视器上（实时运行/单步）；从历史列表浏览旧运行走的是 `runDetail` 摘要表，未驱动时光机。**
- [x] **F-20 断点调试 UI**（M~L）✅ 已完成（VM 暂停/恢复机制 + UI；运行时需 `tauri dev` 验证）
  - 设计：断点 = 复用既有「协作式取消 + F-13 resume + F-19 变量快照」组合，而非常驻暂停进程。
    VM 在某 step **执行前**暂停（不执行、不持久化该 step），运行结束返回 `RunReport.paused_at`；
    「单步/继续」= 以该暂停运行为 `resume_from` 再跑一次（replay 已完成 step，「跨过」暂停点跑到
    下一个暂停点）。好处：可单测（如 cancel/resume）、不漏资源（teardown 每次照常触发）。
  - 后端（lumo-core）：新增 `ExecError::Paused`；`ctx::DebugController`（断点集 + 单步 + `armed`/
    `paused`/`paused_at`，`Arc` 内部共享 → `control.parallel` fork 间共享，一处命中全体短路）；
    `StepCtx` 挂 `debug` 并随 `fork()` 克隆；`FlowVm::with_breakpoints`/`with_step_mode`。
    `execute_step` 两处检查：顶部粘性守卫（命中后所有后续 step 直接短路、清晰穿出 try/parallel），
    resume replay **之后**的断点判定（故 resumed step 快进、且「跨过」上次暂停点）。`run()` 凭
    **ctx 上的 `paused_at` 旗标**（非错误变体——`control.try` 无 catch 路径会把错误重包成 `Other`）
    判定 paused 并回 `Ok(report)`。新增 `tests/breakpoint.rs`（4 测：暂停于断点、resume 续跑、
    单步逐步推进、空断点直跑到底）。
  - 桌面：`debug_flow` Tauri 命令（断点 / 单步 / `resume_from`，强制持久化以供 resume）；
    `execute_flow` 抽出 `DebugOpts`（默认空 ⇒ 老调用零影响）；`RunReportDto.paused_at`→`pausedAt`。
  - 前端：`js/debug.js`（startDebug/step/continue/stop + 暂停态调试条）；`runs.js` 抽出
    `applyRunResponse` 供调试复用时光机 + F-19 变量 watch；inspector 加「设为断点」开关；
    工具栏「🐞 调试」按钮；Time-Travel 面板上方 `#debugBar`（全程 `html()` 转义守 P0-3 CSP）。
  - `cargo test -p lumo-core`（breakpoint 4 + 全套 70+ 绿）、`lumorpa-desktop` `cargo check` 绿、
    前端 `eslint .` 干净。**运行时 UI 冒烟需 `tauri dev`（前端无自动化测试，见 F-21）。已知范围：
    断点经 inspector 设于顶层节点（id 即 path），嵌套节点 / 流程图内联标记延后；调试态复用时光机
    检视器看变量。**
- [x] **F-21 `app.js` 拆 ES 模块 + 重开 lint**(M)✅ 已完成
  - 3175 行 `app.js` 拆为 28 个原生 ES 模块(`js/` 下 api/dom/constants/state/yaml/theme/flows/runs/providers/recorder/… + `js/editor/` 的 steps/graph/tree/code/render/mutations/schema/inspector/ai-drawer,入口 `js/main.js`);`index.html` 改 `<script type="module">`。重开 ESLint 9 flat config(`eslint.config.js` + `package.json` 的 `lint` 脚本),`npx eslint .` 干净。纯结构重构、无行为改动;严守 P0-3 CSP `script-src 'self'`(无内联脚本/eval)。**注:前端无自动化测试覆盖,运行时冒烟需手动 `tauri dev` 验证。**

**录制器 / Schema 生成**
- [x] **F-22 录制器 Drop 优雅关闭 + 连接已有 Chrome + 选择器评分**(M)✅ 已完成
  - `BrowserSession` 改 `Arc<Mutex<Browser>>` + `Drop`/`stop()` 优雅 teardown(close→kill→wait 回收子进程,镜像 P1-2 的 `BrowserTeardown`);`BrowserMode::{Launch, Connect{endpoint}}` + `connect_to` 支持连接已有 Chrome 的 DevTools 端点(连接模式 teardown 只断开、不杀进程);新增 `selector_score.rs` 纯逻辑选择器评分(id > data-* > 稳定 class > nth-child,惩罚易变/自动生成 class)。`cargo test -p lumo-recorder` 41+3 绿、clippy 干净;真 Chrome 行为路径走 `#[ignore]` e2e。
- [x] **F-23 action `schema()` 改 schemars 派生**(满足 README 优先级 #2)(M)✅ 已完成(315d7ee)
  - 92 个 action 的手写 `schema()` 改为 `crate::schema::derive::<XIn>()`,输入结构体成唯一真相源;draft07 + `option_add_null_type=false` + `inline_subschemas=true` + `deny_unknown_fields`。String 枚举重构为 Rust enum(date.diff.unit / http.upload.mode / data.join.type / browser.wait.condition)、`MultiSelector` 派生 JsonSchema 保住嵌套 selectors 封闭。新增双重门禁 `tests/schema_derive.rs`(结构 + schema 驱动行为)。刻意保留 9 个手写(`Value`/嵌套枚举/notify 安全路径,门禁注释逐条记录)。

## ✅ 已经做得好的(勿误判 / 须更新文档)

- 错误模型生产级(`error.rs` typed `ErrorKind` 驱动 AI fallback);全仓库一致 thiserror+anyhow。
- 录制器其实是可用 CDP+桌面录制器(`crates/lumo-recorder/src/lib.rs:341`)— **README "placeholder" 标签须更正**(审计 2026-06-05 已更正)。浏览器录制器为真实实现:chromiumoxide CDP + `Runtime.addBinding` 捕获 click/input/change/keydown、选择器评分、alt-click 抓相似元素、YAML-patch 合并、元素库回填(`crates/lumo-recorder/src/lib.rs:467-617`),非占位。
  - **剩余 TODO(审计 2026-06-05)**:`DesktopRecorder` 尚未接入桌面 app —— 桌面/混合目标的 `recorder_start` 仍回退到 `NoopRecorder`(`apps/desktop/src-tauri/src/lib.rs:1527-1529`);元素库回填目前仅来自 DOM 事件。待办:把 `DesktopRecorder` 接入 `recorder_start` 的 desktop/mixed 分支,让 `desktop.*` 事件喂元素库。
- 影刀式编辑器已实现(`app.js:1303 renderStepList`)。
- 多数 panic 在测试内/已守卫,运行时热路径干净。
- scheduler/cron/file-watch/webhook(`serve.rs`)+ 789 行 MCP server 均已实现 — README 反而低估;设计文档 M3/M4 愿景勿读作现状,建议加"已建/计划"标记。(审计 2026-06-05 复核:scheduler + MCP server 确已实现;多 worker / 云编排 M3/M4 仍未构建。)

## 🎯 执行顺序

1. AI 钩子接线(P0-1)+ hook 成本账本(P1-4)
2. 安全洞:路径规范化(P0-2)+ Tauri IPC/CSP(P0-3)
3. `control.parallel` 隔离(P0-4)+ VM 取消/超时(P1-1)
4. `skill.invoke` 守卫(P0-5)+ `lumo run` 退出码(P1-6)+ 桌面锁中毒(P1-11)
5. 解析器严格化(P1-10)+ 浏览器回收(P1-2)+ 多模态限制/重试(P1-5)
6. 存储迁移框架(P1-7)+ vault(P1-3)
7. 工程化:`lumo-actions` 测试(P1-8)+ CI 加固(P1-9)
8. 功能补充:S 类(剪贴板/ZIP/wait/http.download/通知)→ M 类(email/PDF/数据表/Magic Prompt/变量面板/flow.call/布尔表达式/schemars)→ L 类(桌面自动化/OCR/断点续跑/断点调试)
