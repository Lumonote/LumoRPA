# LumoRPA cargo config.
#
# Keep the repository-level config neutral so contributors and CI use the
# standard crates.io sparse index. If a local mirror is needed, configure it in
# ~/.cargo/config.toml instead of forcing every checkout through one endpoint.

[net]
git-fetch-with-cli = true
retry = 5
